package farmework.gen.demo;

import org.junit.platform.commons.support.AnnotationSupport;

import java.lang.reflect.Method;
import java.lang.reflect.Parameter;
import java.util.Arrays;

public class GenHttpRequestDemo {

    public static void main(String[] args) {
//        Class<?> controllerClass = PingController.class;
//        Method[] methods = controllerClass.getMethods();
//        Arrays.stream(methods)
//                .filter(method -> AnnotationSupport.isAnnotated(method, RequestMapping.class))
//                .forEach(method -> {
//
//                    RequestMapping requestMapping = method.getAnnotation(RequestMapping.class);
//                    String url = requestMapping.value()[0];
//                    System.out.println("url = " + url);
//                    RequestMethod reqMethod = requestMapping.method()[0];
//                    System.out.println("reqMethod.name() = " + reqMethod.name());
//
//                    Parameter[] parameters = method.getParameters();
//                    for (Parameter parameter : parameters) {
//                        System.out.println("parameter.getName() = " + parameter.getName());
//                    }
//                });
    }
}
